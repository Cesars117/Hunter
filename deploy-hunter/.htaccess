# Hunter - Proxy a Node.js Application
# Hostinger redirige automáticamente tráfico al puerto de Node.js
# Este archivo es un respaldo en caso de que se necesite configuración adicional

RewriteEngine On

# Forzar HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Proxy a Node.js (Hostinger maneja esto automáticamente con su configuración Node.js)
# Si necesitas configurarlo manualmente, descomenta las siguientes líneas:
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)$ http://127.0.0.1:3000/$1 [P,L]
